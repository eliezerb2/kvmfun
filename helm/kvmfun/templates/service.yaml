apiVersion: v1
kind: Service
metadata:
  name: {{ include "kvmfun.fullname" . }}
  labels:
    {{- include "kvmfun.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kvmfun.selectorLabels" . | nindent 4 }}